<%= render partial: 'shared/auth_source_image',
           locals: { :type => feed_item['type'], :size => "15x15" } %>

<%= link_to feed_item['user']['username'],
            user_path(feed_item['user']['user_id']) %>

posted <%= time_ago_in_words(feed_item['data']['event_created_at']) %> ago
