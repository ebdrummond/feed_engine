<div id="side_bar">
  <h5>Kreepings</h5>
  <ul>
  <% @kreepings.each do |kreeping| %>
    <li><%= link_to kreeping.name, trip_path(kreeping) %></li>
  <% end %>
  </ul>
  <h6><a href="#">See all</a></h6>
</div>
